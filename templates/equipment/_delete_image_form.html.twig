<form method="post" action="{{ path('equipment.delete.image', {'id': equipment.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer la photo de ce matos ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('deleteImage' ~ equipment.id) }}">
    <button class="btn">
        <i class="fas fa-trash"></i>
    </button>
</form>
